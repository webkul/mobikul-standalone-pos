<?xml version="1.0" encoding="utf-8"?>
<searchable xmlns:android="http://schemas.android.com/apk/res/android"
    android:hint="@string/search_hint"
    android:includeInGlobalSearch="true"
    android:label="@string/app_name"
    android:queryAfterZeroResults="false"
    android:searchMode="queryRewriteFromText"
    android:searchSettingsDescription="string resource"
    android:searchSuggestAuthority="com.webkul.prestashop_app.MySuggestionProvider"
    android:searchSuggestIntentAction="android.intent.action.SEARCH"
    android:searchSuggestSelection=" ?"
    android:searchSuggestThreshold="1"
    android:textColorHint="@color/secondaryTextColor"
    android:voiceLanguage="stringlang"
    android:voiceLanguageModel="free-form"
    android:voiceMaxResults="7"
    android:voicePromptText="string resource"
    android:voiceSearchMode="showVoiceSearchButton|launchRecognizer">

    <actionkey
        android:keycode="KEYCODE_ENTER"
        android:queryActionMsg="string1"
        android:suggestActionMsg="string2"
        android:suggestActionMsgColumn="string3" />

</searchable>